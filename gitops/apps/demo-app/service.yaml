apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: demo-app
  labels:
    app: web
    app.kubernetes.io/name: platform-api
    app.kubernetes.io/part-of: platform-demo
    app.kubernetes.io/component: gateway
    observability: platform-demo
    platform_service: platform-api
spec:
  selector:
    app: web

  ports:
    - name: http
      port: 80
      targetPort: http
